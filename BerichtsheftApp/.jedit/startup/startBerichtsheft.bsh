void doSomethingUseful()
{
    void run()
    {
        view = jEdit.getLastView();

        view.getDockableWindowManager().showDockableWindow("berichtsheft");
    }

    if(jEdit.getLastView() == null)
        VFSManager.runInAWTThread(this);
    else
        run();
}

doSomethingUseful();
