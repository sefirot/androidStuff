<COMMANDO>
	<UI>
		<MEMORY LABEL="Task" VARNAME="task"
			EVAL="&quot;Tasks&quot;" />
			
		<ENTRY LABEL="Parameters" VARNAME="params" />
	</UI>

	<COMMANDS>
		<COMMAND SHELL="System">
			buf = new StringBuffer();
			script = System.getProperty("user.dir") + "/scripts/process.sh";
			buf.append("\"" + script + "\" \"" + task + "\"");
			if (params.length() > 0)
				buf.append(" "+params);
			// return value
			return buf.toString();
		</COMMAND>
	</COMMANDS>
</COMMANDO>
